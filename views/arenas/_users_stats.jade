table.table.table-left.table-striped.table-bordered
  thead
    tr
      th Username
      th WPM
      th Accuracy
  tbody
    - each user, i in arena.users
      tr(data-id='#{user._id}')
        td.username= user.username
        td.wpm 0
        td.accuracy 0%
